include inc/head.pug

div.content
    div.container
        div.container-4
            div.box
                
        div.container-8
            div.box
                h2 Liste des questions

                if questions.length
                    each question in questions
                        div
                            a(href='/q/' + question.id + '-' + question.urlname) #{question.title}
                else
                    p Aucune question
    
include inc/foot.pug
